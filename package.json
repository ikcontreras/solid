{
  "name": "solid",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node build/index.js",
    "dev": "nodemon src/index.ts",
    "start:dev": "cross-env NODE_ENV=development ts-node src/index.ts",
    "start:prod": "cross-env NODE_ENV=production node build/index.js",
    "start:test": "cross-env NODE_ENV=test ts-node src/index.ts",
    "test": "cross-env NODE_ENV=test mocha --recursive --require ts-node/register tests/**/*.ts"
  },
  "dependencies": {
    "dotenv-flow": "^4.1.0",
    "express": "^4.21.1"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/express": "^5.0.0",
    "@types/node": "^22.7.5",
    "cross-env": "^7.0.3",
    "nodemon": "^3.1.7",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  },
  "private": true
}
